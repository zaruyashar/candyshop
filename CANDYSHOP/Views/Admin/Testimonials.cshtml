@model List<CANDYSHOP.Models.Testimonial>
@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Testimonials";
}

<div class="page-head">
  <div class="crumbs">
    <i class="fa-solid fa-house" style="color:var(--pink)"></i>
    <span>Home</span>
    <span class="sep">/</span>
    <span class="here">Testimonials</span>
  </div>
  <a href="@Url.Action("TestimonialCreate", "Admin")" class="btn-add">
    <i class="fa-solid fa-plus"></i> Add New
  </a>
</div>

<div class="card">
  <div class="card-head">
    <i class="fa-solid fa-comment-dots"></i> All Testimonials
  </div>
  <div class="card-body">
    <div class="table-tools">
      <div class="len">
        <label>
          Show
          <select id="pageLen">
            <option selected>10</option>
            <option>25</option>
            <option>50</option>
          </select>
          entries
        </label>
      </div>
      <div class="search">
        <form method="get" action="@Url.Action("Testimonials", "Admin")" style="display: flex; gap: 5px;">
            <input type="search" name="q" id="searchInput" placeholder="Search testimonials..." value="@ViewBag.SearchQuery">
            <button type="submit" class="btn-save" style="padding: 5px 15px; font-size: 13px; border-radius: 4px;">Search</button>
            @if (!string.IsNullOrEmpty(ViewBag.SearchQuery))
            {
                <a href="@Url.Action("Testimonials", "Admin")" class="btn-ghost" style="padding: 5px 10px; text-decoration: none; border-radius: 4px; display: flex; align-items: center;">Clear</a>
            }
        </form>
      </div>
    </div>

    <div class="table-wrap">
      <table class="data" id="testimonialTable">
        <thead>
          <tr>
            <th>#</th>
            <th>Client Name</th>
            <th>Role</th>
            <th>Comment</th>
            <th style="text-align:right">Actions</th>
          </tr>
        </thead>
        <tbody>
          @if (Model == null || !Model.Any())
          {
              <tr>
                  <td colspan="5" style="text-align:center;padding:30px;color:var(--muted)">No testimonials found.</td>
              </tr>
          }
          else
          {
              @foreach (var item in Model)
              {
                  <tr>
                    <td>@item.Id</td>
                    <td><strong>@item.ClientName</strong></td>
                    <td>@item.Role</td>
                    <td style="max-width: 300px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;">@item.Comment</td>
                    <td style="text-align:right">
                      <div class="row-actions">
                        <a href="@Url.Action("TestimonialEdit", "Admin", new { id = item.Id })" class="btn-icon btn-edit" title="Edit">
                          <i class="fa-solid fa-pen"></i>
                        </a>
                        <button class="btn-icon btn-delete" data-id="@item.Id" data-name="@item.ClientName" title="Delete">
                          <i class="fa-solid fa-trash"></i>
                        </button>
                      </div>
                    </td>
                  </tr>
              }
          }
        </tbody>
      </table>
    </div>

    <div class="table-foot">
      <div id="info">Showing @(Model?.Count ?? 0) entries</div>
    </div>
  </div>
</div>

@section Scripts {
<script>
  // SweetAlert delete
  document.querySelectorAll(".btn-delete").forEach(button => {
      button.addEventListener("click", () => {
          const id = button.dataset.id;
          const name = button.dataset.name;
          
          Swal.fire({
              title: `Delete testimonial from "${name}"?`,
              text: "This action cannot be undone.",
              icon: "warning",
              showCancelButton: true,
              confirmButtonColor: "#D0324B",
              cancelButtonColor: "#3d2230",
              confirmButtonText: "Yes, delete it!",
              background: "#FDF5E7"
          }).then(res => {
              if (res.isConfirmed) {
                  fetch(`@Url.Action("TestimonialDelete", "Admin")/${id}`, {
                      method: "POST"
                  })
                  .then(response => response.json())
                  .then(data => {
                      if (data.success) {
                          Swal.fire({
                              title: "Deleted!",
                              icon: "success",
                              background: "#FDF5E7",
                              confirmButtonColor: "#2dbba4"
                          }).then(() => {
                              location.reload();
                          });
                      } else {
                          Swal.fire({
                              title: "Error!",
                              text: data.message || "Failed to delete testimonial.",
                              icon: "error",
                              background: "#FDF5E7"
                          });
                      }
                  });
              }
          });
      });
  });
</script>
}
